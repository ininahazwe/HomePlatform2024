{{ form_start(form) }}
<div class="form-group">{{ form_widget(form.code) }}</div>
<div class="form-group">{{ form_widget(form.sujet) }}</div>
<div class="form-group">{{ form_widget(form.contenu) }}</div>
<button type="submit" class="default-btn border-radius-5 mt-2">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
