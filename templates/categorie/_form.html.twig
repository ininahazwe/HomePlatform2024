{{ form_start(form) }}
<div class="form-group">{{ form_row(form.nom) }}</div>
<div class="form-group">{{ form_row(form.description) }}</div>
{{ form_row(form.logo) }}
    {% if app.request.attributes.get('_route') == 'categorie_edit' %}
        <h2>Logos</h2>
        {% for image in categorie.logo %}
            <div>
                <img src="{{ asset('/uploads/'~image.name) }}" alt="" width="150">
                <a href="{{ path('categories_delete_image', {id: image.id})}}" data-delete data-token="{{ csrf_token('delete' ~ image.id )}}">Supprimer</a>
            </div>
        {% endfor %}
    {% endif %}
    <button class="default-btn border-radius-5">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
