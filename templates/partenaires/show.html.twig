{% extends 'base.html.twig' %}

{% block title %}partenaires{% endblock %}

{% block body %}
    <h1>partenaires</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ partenaires.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ partenaires.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('partenaires_index') }}">back to list</a>

    <a href="{{ path('partenaires_edit', {'id': partenaires.id}) }}">edit</a>

    {{ include('partenaires/_delete_form.html.twig') }}
{% endblock %}
