<form method="post" action="{{ path('partenaires_delete', {'id': partenaires.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ partenaires.id) }}">
    <button class="btn">Delete</button>
</form>
